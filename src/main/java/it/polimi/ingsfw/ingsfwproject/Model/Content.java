package it.polimi.ingsfw.ingsfwproject.Model;

/**
 * This enum class represents every resource and object of the game.
 * It also represents whether a corner is empty or covered.
 * It can be used for corners, card's central item, requested object for placing a card and
 * for all cases in which is necessary to represent a resource or object.
 */
public enum Content {
    PLANT_KINGDOM,
    ANIMAL_KINGDOM,
    FUNGI_KINGDOM,
    INSECT_KINGDOM,
    QUILL,
    INKWELL,
    MANUSCRIPT,
    EMPTY,
    HIDDEN
}
